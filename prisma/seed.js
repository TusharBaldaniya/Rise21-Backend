// Database seeding script
async function main() {
  console.log("🌱 Database is connected and verified. Ready for registrations!");
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  });
